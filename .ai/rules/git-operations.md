# Git Operations

Never automatically commit, push, amend, or create PRs without explicit user confirmation.

Always ask before:
- `git commit`
- `git push`
- `git amend`
- `git rebase`
- `git merge`
- Creating or updating PRs

This also applies to any destructive operations like `git reset`, `git checkout --`, or `git clean`.
